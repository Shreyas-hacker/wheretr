import AppBar from "@mui/material/AppBar";
import Toolbar from '@mui/material/Toolbar';
import React from "react";

const Header = ()=>{
    return (
        <header>
            <AppBar><Toolbar>WheresMyTr?</Toolbar></AppBar>
        </header>
    )
}

export default Header;